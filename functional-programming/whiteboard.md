##.map challenge:

1) Return array.map
2) Insert function into .map (unnamed?)
3) Function needs to multiply index by 2

##.filter challenge:

1) Return birds.filter
2) Function needs to be an "includes", that embodies all the birds possible

##.reduce challenge:
1) Set up a new variable to reduce the array into a sum
2) .reduce needs to add elements a and b
3) Divide sum by array.length
